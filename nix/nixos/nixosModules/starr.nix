{...}: {
  services.prowlarr = {
    enable = true;
  };

  services.radarr = {
    enable = true;
  };

  services.sonarr = {
    enable = true;
  };

  services.whisparr = {
    enable = true;
  };

  services.flaresolverr = {
    enable = true;
  };

  services.jellyfin = {
    enable = true;
  };
}
